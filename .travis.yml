language: java
sudo: false
install: true
addons:
  sonarqube: true

jdk:
  - oraclejdk8

script:
  - echo $SONARQUBE_SCANNER_PARAMS
  - sonar-scanner -Dsonar.login=$SONAR_TOKEN

cache:
  directories:
    - '$HOME/.sonar/cache'

notifications:
  email: false
