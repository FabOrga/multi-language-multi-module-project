language: java
sudo: false
install: true
addons:
  sonarqube:
    organization: "fab"
  

jdk:
  - oraclejdk8

script:
  - sonar-scanner -Dsonar.login=$SONAR_TOKEN

cache:
  directories:
    - '$HOME/.sonar/cache'

notifications:
  email: false
